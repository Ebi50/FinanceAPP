// This file is no longer needed because admin initialization is now handled directly
// within the API route to ensure stability. This prevents module-level code
// from running before the server environment is ready.
// Keeping the file but leaving it empty avoids breaking any potential import paths
// that might exist, although its content is no longer used.
